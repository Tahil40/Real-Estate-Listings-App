import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SignUpPage() {
  return (
    <SafeAreaView>
      <View>
        <Text>Welcome in Create Account page</Text>
      </View>
    </SafeAreaView>
  );
}
