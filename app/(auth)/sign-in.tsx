import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SignInPage() {
  return (
    <SafeAreaView>
      <View>
        <Text>Welcome in login page</Text>
      </View>
    </SafeAreaView>
  );
}